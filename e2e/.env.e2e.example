# E2E Test Environment Variables
# Copy this file to .env.e2e and fill in values

# Test account credentials (create a dedicated test account in Clerk Dashboard)
E2E_USER_EMAIL=test@example.com
E2E_USER_PASSWORD=your-test-password

# Optional: fresh email for sign-up tests (must be unique each run)
# E2E_FRESH_EMAIL=fresh-test-123@example.com

# Optional: OTP test account (account configured for email code verification)
# E2E_OTP_EMAIL=otp-test@example.com
